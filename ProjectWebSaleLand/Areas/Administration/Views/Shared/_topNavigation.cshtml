<div class="top_nav">
    <div class="nav_menu">
        <nav>
            <div class="nav toggle">
                <a id="menu_toggle"><i class="fa fa-bars"></i></a>
            </div>
            <ul class="nav navbar-nav navbar-right">
                <li class="">
                    <a href="javascript:;" class="user-profile dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
                        @*<img src="@Url.Content("~/Assets/images/picture.jpg")" alt="" />*@
                        @{
                            var user = Session["User"] as ProjectWebSaleLand.Shared.Models.UserSession;
                            if (user != null)
                            {
                                if (user.ImageUrl == null || user.ImageUrl.Length == 0) {
                                    <img src="@Url.Content("~/Assets/images/avatarDefault.jpg")" alt="" />
                                    }
                                    else
                                    {
                                        <img src = "@user.ImageUrl" alt = "" />
                                    }
                                    @Html.Raw(user.UserName)
                            }

                            }
                        <span class="fa fa-angle-down"></span>
                    </a>
                    <ul class="dropdown-menu dropdown-usermenu pull-right">
                        @*<li><a href="@Url.Action("Index","Profile", new { area = "" })"> Profile</a></li>*@
                        <li><a href="@Url.Action("Logout", "Home", new { area = "Administration" })"><i class="fa fa-sign-out pull-right"></i>Đăng xuất</a></li>
                    </ul>
                </li>
            </ul>
        </nav>
    </div>
</div>